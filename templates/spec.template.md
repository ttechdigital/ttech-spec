# <Título da spec>

> Contexto: o problema/sistema em ≤3 parágrafos. Use `[NEEDS CLARIFICATION]`, `TODO` ou `???`
> onde houver ambiguidade — o `ttechspec clarify` ranqueia as specs por isso.

## Contexto

## Contrato / schema

## Como adicionar/modificar

## Anti-padrões

## Requirements
<!-- Requisitos com ID estável (FR-/SC-/NFR-###) em forma EARS (WHEN… THE SYSTEM SHALL…). O ID é o que
     liga requisito → task → código. O tipo de regra `spec-traceability` reprova requisito sem task. -->
- **FR-001**: WHEN <evento> THE SYSTEM SHALL <comportamento observável>.
- **SC-001**: <critério de sucesso mensurável>.

## Tasks
<!-- Clareza do que JÁ virou código vs o que falta — checkbox GitHub-padrão. [x] = pronto, [ ] = falta.
     Cite o ID do requisito que a task cobre (FR-001) → o clarify/state medem cobertura.
     O `clarify` mostra feito/total; o `state` agrega por sistema pra plataforma (sem prompt). -->
- [ ] (FR-001) <primeira coisa a construir>

## Checklist antes de subir

## Clarifications
<!-- Preenchido na passada de clarify. Perguntas resolvidas viram decisões aqui. -->

## Histórico
- <data>: criada.
